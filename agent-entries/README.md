# Agent Entries

本目录收纳跨 agent 入口文件，避免仓库根目录过于拥挤。

- `core/`：通用身份、SOUL 和 AGENTS 入口，默认中文，同时提供英文版。
- `hermes/`：Hermes 专用入口。
- `openclaw/`：OpenClaw 专用入口。
- `trae/`：Trae 专用入口。
- `codebuddy/`：CodeBuddy 专用入口。
